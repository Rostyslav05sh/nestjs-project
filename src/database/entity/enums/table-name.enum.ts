export enum TableNameEnum {
  USERS = 'users',
  POSTS = 'posts',
  COMMENTS = 'comments',
  LIKES = 'likes',
  FOLLOW = 'follow',
  BRANDS = 'brands',
  MODELS = 'models',
  REFRESH_TOKENS = 'refresh_tokens',
  ACTION_TOKENS = 'action_tokens',
}
